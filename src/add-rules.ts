import { Rules } from "./create-rules";
import { PluginAPIWrapperObject } from "./plugin-api-wrapper";

function add(rules: Rules, pluginAPIWrapper: PluginAPIWrapperObject) {}

export default {
  add,
};
