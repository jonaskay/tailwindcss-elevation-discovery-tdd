import { ColorsData, ElevationData } from "../plugin-api-wrapper";
import { ConfigData } from "./fetch-config-data";

function map(
  elevationData: ElevationData,
  colorsData: ColorsData
): ConfigData {}

export default { map };
